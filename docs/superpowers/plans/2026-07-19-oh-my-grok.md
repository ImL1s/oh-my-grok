# oh-my-grok MVP Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ship a working Grok Build plugin + `omg` CLI that provides OMC-style workflows (ulw/ralph/ralplan) using **only Grok native `spawn_subagent`**, with dual-review contracts absorbed.

**Architecture:** Option B — `omg` CLI (Python 3, stdlib only) owns hard keywords, state single-writer, acceptance runner; Grok plugin ships `omg-*` skills/agents + hooks (event spool + PreToolUse soft-guard). No fork of grok-build. No tmux in v1.

**Tech Stack:** Python 3.11+, pytest, Grok Build plugin conventions (`plugin.json`, `hooks/hooks.json`, `skills/*/SKILL.md`, `agents/*.md`). Spec: `<grok-build-root>/.omc/research/oh-my-grok-plan.md`. Reviews: `dual-review-codex.md`, `dual-review-fable.md`.

**Repo root:** `<repo-root>` (sibling of grok-build; init git on main).

---

## File map (create / replace)

| Path | Responsibility |
|------|----------------|
| `plugin.json` | Plugin manifest (omg-prefixed keywords) |
| `hooks/hooks.json` | SessionStart, SubagentStop, Stop, **PreToolUse** |
| `hooks/bin/session_start.py` | Ensure `.omg/` dirs; append session event |
| `hooks/bin/subagent_stop.py` | Append subagent stop event |
| `hooks/bin/stop.py` | Append stop event (never mark verified) |
| `hooks/bin/pre_tool_use_deny.py` | External-CLI deny soft-guard |
| `hooks/bin/_common.py` | Paths, atomic JSONL append, workspace root |
| `bin/omg` | CLI entry (python3) |
| `omg_cli/__init__.py` | Package |
| `omg_cli/main.py` | Argparse router |
| `omg_cli/setup_cmd.py` | setup |
| `omg_cli/doctor.py` | doctor |
| `omg_cli/state.py` | run state single-writer |
| `omg_cli/deny.py` | shared deny logic (tested) |
| `omg_cli/modes.py` | ulw / ralph / ralplan launchers (v1 skeleton) |
| `skills/omg-using/SKILL.md` | Bootstrap skill |
| `skills/omg-ultrawork/SKILL.md` | Parallel spawn playbook |
| `skills/omg-ralph/SKILL.md` | Persistence loop playbook |
| `skills/omg-ralplan/SKILL.md` | Plan consensus playbook |
| `skills/omg-cancel/SKILL.md` | Cancel guidance |
| `agents/omg-orchestrator.md` | Orchestrator agent |
| `agents/omg-executor.md` | Executor agent |
| `agents/omg-critic.md` | Critic agent |
| `agents/omg-verifier.md` | Verifier agent |
| `templates/AGENTS.fragment.md` | AGENTS merge fragment |
| `templates/gitignore.fragment` | gitignore fragment |
| `tests/test_deny.py` | Deny regex + env bypass cases |
| `tests/test_state.py` | State writer atomicity |
| `tests/test_hooks_common.py` | Hook helpers |
| `tests/test_cli_router.py` | CLI help / aliases |
| `README.md` | Install + HARD RULES |
| `pyproject.toml` or `requirements-dev.txt` | pytest only |

---

### Task 1: Repo bootstrap + deny module (TDD)

**Files:**
- Create: `omg_cli/__init__.py`, `omg_cli/deny.py`, `tests/test_deny.py`, `requirements-dev.txt`, `README.md`
- Modify: `plugin.json`, `.gitignore`
- Test: `tests/test_deny.py`

- [ ] **Step 1: Write failing tests for deny**

```python
# tests/test_deny.py
import os
import pytest
from omg_cli.deny import should_deny_command, decide_pre_tool_use

@pytest.mark.parametrize("cmd", [
    "claude -p 'hi'",
    "codex exec foo",
    "/usr/local/bin/claude",
    "env claude -p x",
    "OMG_ALLOW_EXTERNAL_CLI=1 claude -p x",  # must STILL deny if process env unset
    "sh -c 'claude -p x'",
    "command codex exec",
    "xargs claude",
    "omc team 2:codex 'x'",
    "agy -p x",
    "cursor-agent -p x",
])
def test_deny_external_cli(cmd):
    assert should_deny_command(cmd) is True

@pytest.mark.parametrize("cmd", [
    "grok -p 'hi'",
    "pytest tests/",
    "python3 scripts/foo.py",
    "git status",
    "echo claude is a word",  # word not as executable head
])
def test_allow_benign(cmd):
    assert should_deny_command(cmd) is False

def test_process_env_allow_only_when_set(monkeypatch):
    monkeypatch.delenv("OMG_ALLOW_EXTERNAL_CLI", raising=False)
    d = decide_pre_tool_use({"toolName": "run_terminal_command", "toolInput": {"command": "claude -p x"}})
    assert d["decision"] == "deny"
    monkeypatch.setenv("OMG_ALLOW_EXTERNAL_CLI", "1")
    d = decide_pre_tool_use({"toolName": "run_terminal_command", "toolInput": {"command": "claude -p x"}})
    assert d["decision"] == "allow"
```

- [ ] **Step 2: Run tests — expect FAIL**

```bash
cd <repo-root>
python3 -m pip install pytest -q
PYTHONPATH=. python3 -m pytest tests/test_deny.py -v
```
Expected: FAIL (module not found)

- [ ] **Step 3: Implement `omg_cli/deny.py`**

```python
# omg_cli/deny.py
from __future__ import annotations
import os
import re
from typing import Any

# Executable names that default workers must not invoke as external agent CLIs
_DENY_BINS = r"(?:claude|codex|omx|agy|cursor-agent|kimi)"
# Allow path prefix, env assigns before bin, shell wrappers
_DENY_RE = re.compile(
    rf"(?:^|[;&|(`\s])(?:(?:[A-Za-z_][\w]*=\S*\s+)*)(?:(?:env|command|xargs)\s+)*(?:(?:\S*/)?){_DENY_BINS}\b"
    rf"|(?:^|[;&|(`\s])omc\s+team\b",
    re.IGNORECASE,
)

def should_deny_command(command: str) -> bool:
    if not command or not isinstance(command, str):
        return False
    # "echo claude" style: deny only when bin-like token is a command position
    if re.search(rf"(?:^|[;&|(`\s])(?:(?:[A-Za-z_][\w]*=\S*\s+)*)(?:\S*/)?{_DENY_BINS}\b", command, re.I):
        return True
    if re.search(r"(?:^|[;&|(`\s])omc\s+team\b", command, re.I):
        return True
    if re.search(r"(?:^|[;&|(`\s])(?:env|command|xargs)\s+(?:\S*/)?(?:claude|codex|omx|agy|cursor-agent)\b", command, re.I):
        return True
    if re.search(r"sh\s+-c\s+['\"].*\b(?:claude|codex)\b", command, re.I):
        return True
    return bool(_DENY_RE.search(command))

def decide_pre_tool_use(event: dict[str, Any]) -> dict[str, str]:
    """Return Grok PreToolUse decision. Fail-safe: emit explicit allow/deny always."""
    try:
        tool = (event.get("toolName") or event.get("tool_name") or "").strip()
        # Claude alias
        if tool in ("Bash", "bash"):
            tool = "run_terminal_command"
        if tool not in ("run_terminal_command", "Shell"):
            return {"decision": "allow"}
        tin = event.get("toolInput") or event.get("tool_input") or {}
        cmd = tin.get("command") if isinstance(tin, dict) else None
        if not isinstance(cmd, str):
            return {"decision": "allow"}
        # ONLY process environment — never parse env from command string
        if os.environ.get("OMG_ALLOW_EXTERNAL_CLI") == "1":
            return {"decision": "allow"}
        if should_deny_command(cmd):
            return {
                "decision": "deny",
                "reason": "oh-my-grok: external agent CLI blocked (use omg ask for advisors; set OMG_ALLOW_EXTERNAL_CLI only in omg ask child)",
            }
        return {"decision": "allow"}
    except Exception as e:
        # Explicit allow with reason logged — caller may still choose deny; fail-open is host policy
        return {"decision": "allow", "reason": f"omg-guard-error:{type(e).__name__}"}
```

Also create empty `omg_cli/__init__.py` and `requirements-dev.txt` with `pytest>=8.0`.

Update `plugin.json` keywords to `omg-ultrawork`, `omg-ralph`, `grok-build`, `orchestration`.

- [ ] **Step 4: Run tests — expect PASS**

```bash
PYTHONPATH=. python3 -m pytest tests/test_deny.py -v
```
Expected: all PASS

- [ ] **Step 5: Commit**

```bash
git add omg_cli tests requirements-dev.txt plugin.json .gitignore
git commit -m "feat: add deny module with bypass-case tests"
```

---

### Task 2: Hook common + four hook scripts + hooks.json

**Files:**
- Create: `hooks/bin/_common.py`, `hooks/bin/session_start.py`, `hooks/bin/subagent_stop.py`, `hooks/bin/stop.py`, `hooks/bin/pre_tool_use_deny.py`
- Modify: `hooks/hooks.json`
- Test: `tests/test_hooks_common.py`

- [ ] **Step 1: Write failing tests for common helpers**

```python
# tests/test_hooks_common.py
import json
from pathlib import Path
from hooks.bin._common import workspace_root, ensure_omg_dirs, append_event

def test_ensure_and_append(tmp_path, monkeypatch):
    monkeypatch.setenv("GROK_WORKSPACE_ROOT", str(tmp_path))
    root = workspace_root()
    assert root == tmp_path
    ensure_omg_dirs(root)
    assert (root / ".omg" / "state").is_dir()
    append_event(root, {"event": "test", "status": "ok"})
    lines = (root / ".omg" / "state" / "events.jsonl").read_text().strip().splitlines()
    assert len(lines) == 1
    assert json.loads(lines[0])["event"] == "test"
```

Note: put `hooks` on PYTHONPATH or make `hooks/bin` importable via path hack in tests:

```python
import sys
sys.path.insert(0, "hooks/bin")
from _common import ...
```

- [ ] **Step 2: Implement `_common.py` and hook scripts**

```python
# hooks/bin/_common.py
from __future__ import annotations
import json, os, sys
from pathlib import Path
from datetime import datetime, timezone

def workspace_root() -> Path:
    for key in ("GROK_WORKSPACE_ROOT", "CLAUDE_PROJECT_DIR", "PWD"):
        v = os.environ.get(key)
        if v:
            return Path(v).resolve()
    return Path.cwd().resolve()

def ensure_omg_dirs(root: Path | None = None) -> Path:
    root = root or workspace_root()
    for sub in ("state", "state/runs", "plans", "research", "handoffs", "artifacts", "ultragoal"):
        (root / ".omg" / sub).mkdir(parents=True, exist_ok=True)
    return root

def append_event(root: Path, payload: dict) -> None:
    ensure_omg_dirs(root)
    path = root / ".omg" / "state" / "events.jsonl"
    row = {
        "ts": datetime.now(timezone.utc).isoformat(),
        "session_id": os.environ.get("GROK_SESSION_ID") or os.environ.get("CLAUDE_SESSION_ID"),
        **payload,
    }
    with path.open("a", encoding="utf-8") as f:
        f.write(json.dumps(row, ensure_ascii=False) + "\n")
        f.flush()
        os.fsync(f.fileno())

def read_hook_event() -> dict:
    try:
        raw = sys.stdin.read()
        return json.loads(raw) if raw.strip() else {}
    except Exception:
        return {}
```

```python
# hooks/bin/session_start.py
#!/usr/bin/env python3
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parent))
from _common import workspace_root, ensure_omg_dirs, append_event, read_hook_event
def main():
    root = ensure_omg_dirs()
    ev = read_hook_event()
    append_event(root, {"event": "SessionStart", "status": "ok", "raw_keys": list(ev.keys())[:20]})
if __name__ == "__main__":
    main()
```

Similar for `subagent_stop.py` (`event`: SubagentStop) and `stop.py` (`event`: Stop — **never** set verified).

```python
# hooks/bin/pre_tool_use_deny.py
#!/usr/bin/env python3
import json, sys
from pathlib import Path
# Import deny from package if installed; else load sibling path
ROOT = Path(__file__).resolve().parents[2]
sys.path.insert(0, str(ROOT))
from omg_cli.deny import decide_pre_tool_use

def main():
    try:
        raw = sys.stdin.read()
        event = json.loads(raw) if raw.strip() else {}
    except Exception:
        event = {}
    decision = decide_pre_tool_use(event)
    # Always print JSON decision
    sys.stdout.write(json.dumps(decision) + "\n")
    if decision.get("decision") == "deny":
        sys.exit(2)
    sys.exit(0)

if __name__ == "__main__":
    main()
```

`hooks/hooks.json`:

```json
{
  "hooks": {
    "SessionStart": [{"hooks": [{"type": "command", "command": "python3 \"${GROK_PLUGIN_ROOT}/hooks/bin/session_start.py\"", "timeout": 10}]}],
    "SubagentStop": [{"hooks": [{"type": "command", "command": "python3 \"${GROK_PLUGIN_ROOT}/hooks/bin/subagent_stop.py\"", "timeout": 10}]}],
    "Stop": [{"hooks": [{"type": "command", "command": "python3 \"${GROK_PLUGIN_ROOT}/hooks/bin/stop.py\"", "timeout": 10}]}],
    "PreToolUse": [{
      "matcher": "run_terminal_command|Bash|Shell",
      "hooks": [{"type": "command", "command": "python3 \"${GROK_PLUGIN_ROOT}/hooks/bin/pre_tool_use_deny.py\"", "timeout": 5}]
    }]
  }
}
```

- [ ] **Step 3: chmod +x, run pytest**

```bash
chmod +x hooks/bin/*.py
PYTHONPATH=. python3 -m pytest tests/ -v
```

- [ ] **Step 4: Manual hook smoke**

```bash
echo '{}' | python3 hooks/bin/session_start.py
echo '{"toolName":"run_terminal_command","toolInput":{"command":"claude -p x"}}' | python3 hooks/bin/pre_tool_use_deny.py ; echo exit:$?
# Expected: decision deny, exit 2
```

- [ ] **Step 5: Commit**

```bash
git add hooks tests
git commit -m "feat: lifecycle hooks + PreToolUse external-CLI soft-guard"
```

---

### Task 3: Skills `omg-*` (Grok-native only)

**Files:**
- Create: `skills/omg-using/SKILL.md`, `skills/omg-ultrawork/SKILL.md`, `skills/omg-ralph/SKILL.md`, `skills/omg-ralplan/SKILL.md`, `skills/omg-cancel/SKILL.md`

- [ ] **Step 1: Write each SKILL.md with HARD RULE block**

Every skill MUST contain:

```markdown
## HARD RULES (non-negotiable)
- Fan-out ONLY via Grok `spawn_subagent` (depth=1; children must NOT spawn).
- NEVER invoke claude/codex/omc team/agy/cursor-agent as default workers.
- Use Grok tool names: read_file, search_replace, run_terminal_command, spawn_subagent, grep, list_dir.
- Write-heavy work: isolation worktree + background true; wait with wait_commands_or_subagents / get_command_or_subagent_output.
- State: only omg CLI is authoritative for passes/verified; you may write proposals under .omg/artifacts/.
```

`omg-ultrawork`: decompose → parallel spawn_subagent → integrate notes → leader verification.  
`omg-ralph`: refine prd proposal → implement one story → stop; outer CLI loop continues.  
`omg-ralplan`: plan → critic → revise → verifier (read-only).  
`omg-cancel`: document PID cancel via omg cancel.  
`omg-using`: when to load which skill.

- [ ] **Step 2: Grep guard**

```bash
! rg -n 'oh-my-claudecode:|Task\(|subagent_type=.claude' skills/
# Expected: no matches (exit 1 from rg is success for our purpose)
rg -n 'spawn_subagent' skills/ | head
```

- [ ] **Step 3: Commit**

```bash
git add skills
git commit -m "feat: add omg-* Grok-native workflow skills"
```

---

### Task 4: Agents `omg-*`

**Files:**
- Create: `agents/omg-orchestrator.md`, `agents/omg-executor.md`, `agents/omg-critic.md`, `agents/omg-verifier.md`

Use Grok agent frontmatter:

```markdown
---
name: omg-executor
description: Implements code changes for oh-my-grok tasks. Use for write-heavy workstreams under ULW/RALPH.
prompt_mode: full
permission_mode: default
agents_md: true
---
```

Body: role, success criteria, HARD RULES, no nested spawn.

- [ ] **Step 1: Create four agents**  
- [ ] **Step 2: Confirm no name collision with explore/plan/general-purpose**  
- [ ] **Step 3: Commit** `feat: add omg-* custom agents`

---

### Task 5: CLI router + setup + doctor + state (TDD)

**Files:**
- Create: `bin/omg`, `omg_cli/main.py`, `omg_cli/setup_cmd.py`, `omg_cli/doctor.py`, `omg_cli/state.py`, `templates/AGENTS.fragment.md`, `templates/gitignore.fragment`, `tests/test_state.py`, `tests/test_cli_router.py`

- [ ] **Step 1: Tests for state single-writer**

```python
# tests/test_state.py
from pathlib import Path
from omg_cli.state import create_run, write_status, load_active_run

def test_create_run_atomic(tmp_path):
    run = create_run(tmp_path, mode="ralph", goal="test")
    assert run["status"] == "initialized"
    assert (tmp_path / ".omg" / "state" / "runs" / run["run_id"]).is_dir()
    active = load_active_run(tmp_path)
    assert active["run_id"] == run["run_id"]
    write_status(tmp_path, run["run_id"], "running")
    assert load_active_run(tmp_path)["status"] == "running"
```

- [ ] **Step 2: Implement state.py + main.py CLI**

`bin/omg`:

```python
#!/usr/bin/env python3
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).resolve().parents[1]))
from omg_cli.main import main
if __name__ == "__main__":
    raise SystemExit(main())
```

Commands: `setup`, `doctor`, `state`, `cancel`, `ulw`, `ralph`, `ralplan`, `--help`, `--safe`, `--yolo`.

`setup`: create dirs, write/merge AGENTS fragment, write gitignore fragment, print install instructions (`grok plugin install . --trust`).

`doctor`: check grok on PATH, plugin.json valid, hooks scripts exist+executable, PreToolUse present, skill names omg-*, deny module importable; exit non-zero on fail.

- [ ] **Step 3: pytest all**  
- [ ] **Step 4: Manual CLI**

```bash
chmod +x bin/omg
./bin/omg --help
./bin/omg setup
./bin/omg doctor ; echo exit:$?
./bin/omg state
```

- [ ] **Step 5: Commit** `feat: omg CLI setup/doctor/state skeleton`

---

### Task 6: Mode launchers (ulw / ralph / ralplan skeleton)

**Files:**
- Create: `omg_cli/modes.py`
- Modify: `omg_cli/main.py`
- Test: `tests/test_modes.py` (mock subprocess)

- [ ] **Step 1: Test that mode builds prompt with skill body + HARD RULE and does not include yolo by default**

```python
def test_build_launch_argv_no_yolo_by_default():
    from omg_cli.modes import build_grok_argv
    argv = build_grok_argv(mode="ulw", goal="fix tests", yolo=False, cwd="/tmp/proj")
    assert argv[0] == "grok"
    assert "-p" in argv
    assert "--yolo" not in argv and "bypassPermissions" not in " ".join(argv)
    assert any("spawn_subagent" in a or "HARD RULE" in a or "omg-ultrawork" in a for a in argv)
```

- [ ] **Step 2: Implement build_grok_argv + run_mode** (subprocess.run with timeout; record PID file under run dir)  
- [ ] **Step 3: Ralph loop skeleton** — create run, write prd scaffold JSON, max_iter loop calling grok -p, after each call run frozen acceptance if present; without verifier artifact do not set verified  
- [ ] **Step 4: pytest + commit** `feat: mode launchers for ulw/ralph/ralplan`

---

### Task 7: Plugin validate + install smoke + README

**Files:**
- Modify: `README.md`
- Verify: `grok plugin validate`

- [ ] **Step 1: Run**

```bash
cd <repo-root>
grok plugin validate .
```
Expected: exit 0 / valid

- [ ] **Step 2: Full pytest**

```bash
PYTHONPATH=. python3 -m pytest tests/ -v
```

- [ ] **Step 3: README** — install, HARD RULES, doctor, modes, dual-review links  
- [ ] **Step 4: Commit** `docs: README and plugin validate green`

---

### Task 8: Final verification + code review gate

- [ ] **Step 1: verification-before-completion**

```bash
cd <repo-root>
PYTHONPATH=. python3 -m pytest tests/ -v
grok plugin validate .
./bin/omg doctor || true
./bin/omg --help
# Deny smoke:
echo '{"toolName":"run_terminal_command","toolInput":{"command":"OMG_ALLOW_EXTERNAL_CLI=1 claude -p x"}}' \
  | python3 hooks/bin/pre_tool_use_deny.py ; test $? -eq 2
rg -n 'oh-my-claudecode:|Task\(' skills/ agents/ && exit 1 || true
```

- [ ] **Step 2: requesting-code-review** on full diff vs initial empty commit  
- [ ] **Step 3: Fix Critical/Important from review**  
- [ ] **Step 4: Re-run verification commands (fresh evidence)**  

---

## Spec coverage checklist (self-review)

| Spec requirement | Task |
|------------------|------|
| PreToolUse soft-guard + env-only allow | T1, T2 |
| Fail-open honesty + tests | T1 |
| omg-* skills/agents only | T3, T4 |
| No explore shadow | T4 |
| State single-writer runs/ | T5 |
| setup/doctor | T5 |
| Mode launchers no default yolo | T6 |
| Ralph incomplete without verifier | T6 skeleton + later |
| plugin validate | T7 |
| Adversarial deny cases | T1 |

## Out of scope this plan (defer)

- Full worktree integrator production path  
- Full ralplan multi-round FSM polish  
- omg ask Phase 2  
- tmux team  
- Subagent PreToolUse coverage spike evidence file  

---

**After plan save:** Execute via **subagent-driven-development** (user already requested). Continuous execution without mid-task human check-ins unless BLOCKED.
