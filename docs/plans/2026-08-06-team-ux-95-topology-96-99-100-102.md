# Team UX Topology (#96/#99/#102/#100) Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Default inside-tmux Team launch keeps leader + workers in the same window; provider-aware readiness; topology-preserving scale; silent worker bootstrap.

**Architecture:** Replace `_launch_first_inside` `new-window` with `split-window -h -d -t <leader>`; persist `view_mode=same_window|dedicated_window`; scale adds splits when `same_window`; two-phase readiness (transport ACK vs provider ready); suppress worker-ready JSON / nested `.omg` warnings in panes.

**Tech Stack:** Existing `omg_cli/team/{tmux,plane,runtime,scaling}.py` + hermetic fakes.

**Depends on:** PR #106 (#97/#98) merged or stacked.

---

### Task 1: Same-window default (#96)

**Files:** `omg_cli/team/tmux.py`, tests

Inside default: `split-window -h -d -t <leader_pane>` for first worker, then more splits; `select-layout` on leader's window; restore `select-pane -t leader`. Dedicated window via explicit `--dedicated-window` / `view_mode`.

### Task 2: Scale/relaunch topology (#102)

**Files:** `omg_cli/team/scaling.py`

`_scale_up` / relaunch honor `view_mode`; same_window → split into leader/team window, not `new-window`.

### Task 3: Provider-ready + silent bootstrap (#99/#100)

**Files:** `omg_cli/team/runtime.py`, `plane.py`

Two-phase readiness; stop printing worker-ready JSON into panes; suppress nested project-root warn.
